handler: src/handlers/approvals/handler
events:
    - http:
          path: approvals-backend/{userId}/{referenceId}
          method: get
          cors: ${self:custom.corsheaders}
          request:
              parameters:
                  paths:
                      userId: true
                      referenceId: true
